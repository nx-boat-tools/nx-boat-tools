{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Publish executor",
  "description": "",
  "type": "object",
  "properties": {
    "projectHelmPath": {
      "type": "string",
      "description": "The path to the helm directory of a project"
    },
    "outputPath": {
      "type": "string",
      "description": "The dist path of the project where the chart archive file should be created"
    }
  },
  "required": ["projectHelmPath", "outputPath"]
}
