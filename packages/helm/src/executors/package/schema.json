{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Publish executor",
  "description": "",
  "type": "object",
  "properties": {
    "projectHelmPath": {
      "type": "string",
      "description": "The directory containing the project's helm files may be found"
    },
    "outputPath": {
      "type": "string",
      "description": "The directory to save the resulting version file"
    }
  },
  "required": ["projectHelmPath","outputPath"]
}
