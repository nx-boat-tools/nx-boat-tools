{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Helm Uninstall executor",
  "description": "",
  "type": "object",
  "properties": {
    "dryRun": {
      "type": "boolean",
      "default": false,
      "description": "Whether or not to perferom a dry run instead of actually installing."
    }
  }
}
