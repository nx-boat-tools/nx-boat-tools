{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Copy values files executor",
  "description": "",
  "type": "object",
  "properties": {
    "projectHelmPath": {
      "type": "string",
      "description": "The path to the helm directory of a project"
    },
    "outputPath": {
      "type": "string",
      "description": "The dist path of the project where the values files should be copied to"
    }
  },
  "required": ["projectHelmPath", "outputPath"]
}
