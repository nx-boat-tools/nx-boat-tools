{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Chain Executor",
  "description": "chain executors together as one",
  "type": "object",
  "properties": {
    "targets": {
      "type": "array",
      "description": "An array containing the other executors to call."
    },
    "preTargets": {
      "type": "array",
      "description": "An array containing additional builders to call before the main target block."
    },
    "postTargets": {
      "type": "array",
      "description": "An array containing additional builders to call after the main target block."
    },
    "stages": {
      "type": "object",
      "description": "The stage definitions for the chain."
    },
    "run": {
      "type": "array",
      "description": " An array what stages to run."
    }
  },
  "additionalProperties": true
}
