{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "MinikubeBuild executor",
  "description": "build a docker image inside of minikube",
  "type": "object",
  "properties": {
    "buildPath": {
      "type": "string",
      "description": "The build path of the project"
    },
    "dockerFilePath": {
      "type": "string",
      "description": "The path to the dockerfile for the project "
    }
  },
  "required": ["buildPath"]
}
