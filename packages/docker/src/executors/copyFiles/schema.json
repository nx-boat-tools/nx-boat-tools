{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Copy Docker Files executor",
  "description": "",
  "type": "object",
  "properties": {
    "dockerFilePath": {
      "type": "string",
      "description": "The path to the dockerfile for the project "
    },
    "dockerIgnorePath": {
      "type": "string",
      "description": "The path to the dockerignore for the project"
    },
    "distPath": {
      "type": "string",
      "description": "The dist path of the project where the files should be copied to"
    }
  },
  "required": ["dockerFilePath", "distPath"]
}
