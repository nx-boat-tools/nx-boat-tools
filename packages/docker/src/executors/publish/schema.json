{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Publish executor",
  "description": "",
  "type": "object",
  "properties": {
    "buildPath": {
      "type": "string",
      "description": "The path to the project's `dockerfile`"
    },
    "dockerRepoOrUser": {
      "type": "string",
      "description": "This is the user or repo to publish the image to"
    }
  },
  "required": ["dockerRepoOrUser"]
}
