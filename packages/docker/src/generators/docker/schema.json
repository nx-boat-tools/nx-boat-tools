{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "Docker",
  "title": "",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "The name of the project to add docker to"
    },
    "dockerRepoOrUser": {
      "type": "string",
      "description": "the user or repo to upload the docker image to"
    },
    "minikube": {
      "type": "boolean",
      "description": "Whether or not to add additional targets for minikube.",
      "default": true
    }
  },
  "required": ["project", "dockerRepoOrUser"]
}
