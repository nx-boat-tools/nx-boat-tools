{
  "root": "packages/docker",
  "sourceRoot": "packages/docker/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "options": {
        "lintFilePatterns": ["packages/docker/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/packages/docker"],
      "options": {
        "jestConfig": "packages/docker/jest.config.js",
        "passWithNoTests": true,
        "coverage": true,
        "coverageReporters": ["cobertura"]
      }
    },
    "build": {
      "executor": "@nrwl/node:package",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/docker",
        "tsConfig": "packages/docker/tsconfig.lib.json",
        "packageJson": "packages/docker/package.json",
        "main": "packages/docker/src/index.ts",
        "assets": [
          "packages/docker/*.md",
          {
            "input": "./packages/docker/src",
            "glob": "**/!(*.ts)",
            "output": "./src"
          },
          {
            "input": "./packages/docker/src",
            "glob": "**/*.d.ts",
            "output": "./src"
          },
          {
            "input": "./packages/docker",
            "glob": "generators.json",
            "output": "."
          },
          {
            "input": "./packages/docker",
            "glob": "executors.json",
            "output": "."
          }
        ]
      }
    },
    "updateDependencies": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "if [[ '{args.pathPrefix}' = 'undefined' ]] ; then pathPrefix=''; else pathPrefix='{args.pathPrefix}/'; fi; node ./scripts/update-dependencies.js --root=$PWD --peerVersion='{args.peerVersion}' --excludedRootDeps=guid-typescript,xmlbuilder2 --projectDir=${pathPrefix}packages/docker"
      }
    },
    "version": {
      "executor": "@nrwl/workspace:run-commands",
      "outputs": [],
      "options": {
        "command": "if [[ '{args.pathPrefix}' = 'undefined' ]] ; then pathPrefix=''; else pathPrefix='{args.pathPrefix}/'; fi; cd ${pathPrefix}packages/docker; npm version {args.to} --git-tag-version=false"
      }
    }
  }
}
